#!/bin/sh
#
# logger -t udhcp.d Interface ${interface} ${J_V_interface} IPaddress ${ip} Lease ${lease} ${dhcptype} ${message}

if [ ${interface} = 'eth0' ] && [ ! -z ${ip} ] 
then
	sh <path_to>/loopia.sh your_domain ${ip}
fi
